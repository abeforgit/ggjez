player = {
    x = 16,
    y = 16,

    xVelocity = 0,
    yVelocity = 0,
    acc = 100,
    maxSpeed = 600,
    friction = 20,
    gravity = 80,

    isJumping = false,
    isGrounded = false,
    hasReachedMax = false,
    jumpAcc = 500,
    jumpMaxSpeed = 9.5,

    img = nil
}

return player